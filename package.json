{
    "name": "endy",
    "displayName": "Endy (deprecated)",
    "description": "DEPRECATED: Please use the Smart Keys extension (c75.smart-keys).",
    "version": "0.0.5",
    "publisher": "c75",
    "engines": {
        "vscode": "^1.10.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:endy.goEnd"
    ],
    "main": "./extension",
    "contributes": {
        "commands": [
            {
                "command": "endy.goEnd",
                "title": "go to the end of current line"
            }
        ]
    },
    "scripts": {
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "typescript": "^2.0.3",
        "vscode": "^1.0.0",
        "mocha": "^2.3.3",
        "eslint": "^3.6.0",
        "@types/node": "^6.0.40",
        "@types/mocha": "^2.2.32"
    }
}
