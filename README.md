# endy
Smart `end` key.

## Configuration

Add these lines to your `keybindings.json` and your `End` key will become smarter :)
```
    {
        "key": "end",
        "command": "endy.goEnd",
        "when": "editorTextFocus"
    }
```    